<%= form_for setup_whiskey(@whiskey) do |f| %>
  <% if @whiskey.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@whiskey.errors.count, "error") %> prohibited this whiskey from being saved:</h2>

      <ul>
      <% @whiskey.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :distillery_id, :Distillery %><br />
    <%= f.select :distillery_id, get_distilleries_to_id_hash %>
  </div>

  <div class="field">
    <%= f.label :category_id, :Category %><br />
    <%= f.select :category_id, get_categories_to_id_hash %>
  </div>

  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
